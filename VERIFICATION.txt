OSU!BAND verification — 2026-09-07
=================================

Scope
-----
This archive contains the updated OSU!BAND client/loader sources and the
unified website/control source.

Website/control checks
----------------------
- npm test: 10/10 PASS.
- JavaScript syntax checks pass for the rewritten account/i18n/platform files.
- Key redemption is covered through the real Worker handler and local SQLite:
  a normal Discord user receives an active subscription after a valid key.
- Community chat, public profiles, unified config metadata, admin users,
  Discord-ID moderation, pending .cfg review and runtime settings are covered.
- Migration 0007 adds config channel metadata and community_messages.

Portable C++ checks
-------------------
- tools/test_core.cpp: PASS (session lease, denial/expiry, config roundtrip).
- tools/test_relax.cpp: PASS (60-minute simulated map; bounded queue and input
  edge cases exercised).
- tools/render_ui.cxx compiles against the included Dear ImGui source and was
  used to render/check Config, Beta, Settings and Loader pages, including the
  dimmed light theme and scaled menu.

Runtime changes checked in source
---------------------------------
- single-instance Win32 named mutex for OSUBAND.exe;
- configurable menu key, with F8 reserved;
- persisted 75–135% menu scale with display-size clamping;
- compact Beta watermark;
- unified in-client config list with role/channel/date metadata;
- Open osu!lazer next to Launch OSU BAND in the loader Session page.

Windows build note
------------------
This environment does not contain MSVC + Windows SDK, so the final Windows
OSUBAND.exe and OSUBAND.Loader.exe were not built or executed here. Build the
solution as Release x64 on Windows/Visual Studio 2022 and do the final runtime
smoke test there.
